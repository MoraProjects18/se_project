<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../common/head', {title:"Receptionist"}); %>
</head>

<body>
<div class="container">
    <h2>Tickets Open Today</h2><br>
    <div class="card-columns">
        <% if(data.dataFound){ %>
            <% for(var ti of ticket){ %>

                <div class="card border-success">
                    <div class="card-header bg-success border-success font-weight-bolder">
                        Ticket ID : <%= ti.ticket_id %>
                    </div>
                    <div class="card-body" style="background-color: #b2e4bd;">
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item">User Email&emsp;&emsp;&emsp;: <%=ti.email%></li>

                            <li class="list-group-item">Date and Time&emsp;: <%= ti.start_time %></li>
                            <li class="list-group-item">Status&emsp; &emsp;   &emsp;    &emsp;: <%= ti.status %></li>
                        </ul>
                    </div>
                </div>


            <% } %>
        <% } else {%>
            <div><h4><%= data.error.message%></h4></div>
        <% } %>
    </div>
</div>

</body>
</html>