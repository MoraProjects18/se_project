<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/heads/head'); %>
</head>

<body class="d-flex flex-column min-vh-100">

<header>
    <%- include('../partials/navs/nav'); %>
</header>

<div class="Center">
    <h2>Your Tickets</h2><br>
    <div class="card-columns">
        <% if(data.dataFound){ %>
            <% for(var ti of ticket){ %>

                <div class="card border-success">
                    <div class="card-header bg-success border-success font-weight-bolder">
                        Ticket ID : <%= ti.ticket_id %>
                    </div>
                    <div class="card-body" style="background-color: #b2e4bd;">
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item">Branch&emsp;&emsp;   &emsp;    &emsp;: <%=ti.branch_name%></li>

                            <li class="list-group-item">Date        &emsp;  &emsp;   &emsp;    &emsp;  : <%= (ti.start_date).substring(0, 10) %></li>
                            <li class="list-group-item">Time        &emsp;  &emsp;   &emsp;    &emsp;  : <%= (ti.start_time).substring(0,5) %></li>
                            <li class="list-group-item">Status&emsp; &emsp;   &emsp;    &emsp;: <%= ti.status %></li>
                        </ul>
                    </div>
                </div>


            <% } %>
        <% } else {%>
            <div><h4><%= data.error.message%></h4></div>
        <% } %>
    </div>
</div>

</body>
</html>